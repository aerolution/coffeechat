<% include partials/header %>

    <!-- Page Content -->
    <div class="invitation">
        <div class="invitation-header">
            Greetings! You've been invited to join
        </div>
        <div class="invitation-info">
            <div class="col">
                <div class="group-avatar"><img src="/img/placeholder-avatar2.jpg" /></div>
            </div>
            <div class="col">
                <h6 class="mb-10"><%= channel.channel_name %></h6>
                <ul class="group-stats">
                    <li><%= channel.participant.length %> users registered</li>
                    <% var count = 0; %>
                    <% channel.participant.forEach(function(participant){ %>
                        <% if(participant.online === true){ %>
                            <% count++ ; %>
                        <% } %>
                    <% }); %>
                   <% if(channel.verified === true){ %>    
                    <li><img src="https://cdn.glitch.com/fed07a0d-f89e-468a-b25a-f38a95ed433a%2F9A06D28D-70D9-4658-ABFB-2AAADD203ADD.png?v=1601332181879" height="20" width="22"> Verified Server</li>
                  
                  <% } %>
                  <% if(channel.partnered === true){ %>    
                    <li><img src="https://canary.discordapp.com/assets/6203964d9d25c394a573fd4f6be36d97.svg" height="20" width="30">Partnered Server</li>
                  
                  <% } %>

                </ul>
                <a href="/" class="decline link-underline">Decline Invitation</a>
            </div>
        </div>
        <div class="invitation-join">
                <form action="/channel/join/<%= channel._id %>" method="POST">
                    <button type="submit" class="btn btn-block btn-primary">Join</button>
                </form> 
        </div>
    </div>


<% include partials/footer %>

